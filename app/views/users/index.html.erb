<div class="d-flex justify-content-between align-items-center mb-3">
  <h1>👤 Usuários</h1>
  <%= link_to "Novo Usuário", new_user_path, class: "btn btn-primary" %>
</div>

<table class="table table-striped table-hover">
  <thead class="table-dark">
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td>
          <%= link_to "Ver", user_path(user), class: "btn btn-sm btn-outline-primary" %>
          <%= link_to "Editar", edit_user_path(user), class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "Excluir", user_path(user), method: :delete, data: { confirm: "Tem certeza?" }, class: "btn btn-sm btn-outline-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
