<div class="d-flex justify-content-between align-items-center mb-3">
  <h1>📖 Leituras</h1>
  <%= link_to "Nova Leitura", new_reading_path, class: "btn btn-primary" %>
</div>

<table class="table table-striped table-hover">
  <thead class="table-dark">
    <tr>
      <th>Usuário</th>
      <th>Livro</th>
      <th>Status</th>
      <th>Avaliação</th>
      <th>Comentários</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @readings.each do |reading| %>
      <tr>
        <td><%= reading.user.name %></td>
        <td><%= reading.book.title %></td>
        <td><%= reading.status %></td>
        <td><%= reading.rating || '-' %></td>
        <td><%= truncate(reading.review, length: 40) %></td>
        <td>
          <%= link_to "Ver", reading_path(reading), class: "btn btn-sm btn-outline-primary" %>
          <%= link_to "Editar", edit_reading_path(reading), class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "Excluir", reading_path(reading), method: :delete, data: { confirm: "Tem certeza?" }, class: "btn btn-sm btn-outline-danger" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
